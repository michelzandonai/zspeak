import SwiftUI

/// Overlay flutuante para indicar estado de gravação (futuro)
/// Por enquanto, o feedback visual é feito pelo ícone do menu bar
struct OverlayView: View {
    let state: AppState.RecordingState

    var body: some View {
        // Placeholder — será implementado na Etapa 7 do plano
        EmptyView()
    }
}
